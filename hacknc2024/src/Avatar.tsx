import React, { ReactNode} from 'react';

import { Box } from '@chakra-ui/react';

interface Props {
    children: ReactNode;
    destination?: string
}

const Avatar = ({}: Props) => {
    return(
        <Box>

        </Box>
    );
};

export default Avatar;
